# Chess-move-suggestions
A browser extension that reads the current board state from a chess website's DOM, sends it to a local Stockfish engine, and displays the best move suggestions as an overlay — without making any moves automatically.
